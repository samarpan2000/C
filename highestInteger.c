#include <stdio.h>
int main()
{
    int i = 0, j = 0, choice = 0, num = 0,prev=0;
    printf("enter the number integers you want to put ");
    scanf("%d", &choice);

    for (i = 1; i <= choice; i++)
    {
        for (j = 1; j <= choice; j++)
        {
            printf("enter any  integer ");
            scanf("%d", &num);
        prev=num;
        }
        if(num)
    }
    return 0;
}